package project.utils.data_structures;

public class InfoPacket {
	
	/*
	 * Info:
	 * Data structure which only passes then necessary information representing the hardware device (floor button, elevator button, etc.) to the scheduler
	 */
	
}
